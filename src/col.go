package src

/** Returns a matrix B whose columns span the column space of A */

func col(m *Matrix) (*Matrix, string) {
    return nil, "";
}

/**
// Gets indices of pivot columns in m 
func getPivotColumns(m *Matrix) []int {
    cols := make([]int, m.N);
    reduced, _ := rref(m);
    for i := 0; i < len(reduced.rows); i++ {
        
    }
}*/
